$(document).ready(function () {
    // Modal For Load Partial , Form Must Be Submited , Handler That Must Request To it , Datatable Object(can be null) , Table Wanted To Reaload After Successfully Result(can be null)
    PartialModalLoaderWithPost('userModal', 'editUserForm', 'Edit', null, 'usersTable');
});
